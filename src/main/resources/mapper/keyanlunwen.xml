<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.graduate_sever.Dao.KeYanLunWenMapper">
    <!--产学研实体映射-->
    <resultMap id="KeYanLunWen" type="com.example.graduate_sever.model.KeYanLunWen">
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="status" jdbcType="INTEGER" property="status"/>
        <result column="finishtime" jdbcType="VARCHAR" property="finishtime"/>
        <result column="name" jdbcType="VARCHAR" property="name"/>
        <result column="partment" jdbcType="VARCHAR" property="partment"/>
        <result column="badge" jdbcType="INTEGER" property="badge"/>
        <result column="shenbao" property="shenbao"/>
        <result column="metails" property="metails"/>
    </resultMap>
    <resultMap id="KeYanLunWenMyShenBaoModel" type="com.example.graduate_sever.common.People">
        <result column="name" property="name"/>
        <result column="id" property="id"/>
        <result column="partment" property="partment"/>
        <result column="finishtime" property="finishtime"/>
    </resultMap>
    <!--参与人映射-->
    <resultMap id="KeYanLunWenz_people" type="com.example.graduate_sever.common.People">
        <result column="badeg" jdbcType="INTEGER" javaType="int" property="badge"/>
        <result column="tea_name" jdbcType="VARCHAR" javaType="string" property="name"/>
    </resultMap>
    <resultMap id="count6" type="java.lang.Long"/>
    <resultMap id="KeYanLunWenMetails" type="com.example.graduate_sever.common.Metails">
        <result column="metails" property="metails"></result>
    </resultMap>
    <!--查询产学研所有成果-->
    <!--application.yml 中通过 type-aliases-package 指定了实体类的为了，因此-->
    <select id="getAllKeYanLunWen" resultMap="KeYanLunWen,count6">
        <bind name="beginIndex" value="(pageIndex-1)*pageSize"></bind>
        select keyanlunwen.* from keyanlunwen where status=1 limit #{beginIndex,jdbcType=INTEGER},#{pageSize,jdbcType=INTEGER};select count(*) count6 from keyanlunwen where status=1;
    </select>
    <!--删除一个产学研成果-->
    <delete id="deleteOneKeYanLunWen">
        delete * from keyanlunwen where keyanlunwen.id=#{id}
    </delete>
    <!--查询指定产学研成果人员信息-->
    <select id="getKeYanLunWenDetail" parameterType="java.lang.Integer" resultMap="KeYanLunWenz_people">
        select teacher.tea_name,teacher.badeg from participation,teacher where participation.badge=teacher.badeg and participation.ach_id=#{id} and participation.type=10
    </select>
<!--    &lt;!&ndash;产学研成果全局模糊查询&ndash;&gt;-->
    <select id="getSearchKeYanLunWen" resultMap="KeYanLunWen,count6">
        <bind name="beginIndex" value="(pageIndex-1)*pageSize"></bind>
        select c.* from keyanlunwen c,teacher t,participation p where status=1 and p.badge=t.badeg and p.ach_id=c.id and CONCAT(c.id,t.tea_name,c.partment,c.name,c.finishtime,c.status,p.badge) LIKE CONCAT('%',#{key},'%') limit #{beginIndex,jdbcType=INTEGER},#{pageSize,jdbcType=INTEGER};
        select count(*) count6 from keyanlunwen c,teacher t,participation p where status=1 and p.badge=t.badeg and p.ach_id=c.id and CONCAT(c.id,t.tea_name,c.partment,c.name,c.finishtime,c.status,p.badge) LIKE CONCAT('%',#{key},'%');
    </select>

    <insert id="insertKeYanLunWen" parameterType="com.example.graduate_sever.entity.HeBingEntity" useGeneratedKeys="true" keyProperty="id">
        INSERT IGNORE INTO keyanlunwen( `status`, `name`, `finishtime`, `partment`,`badge`) VALUES (#{status,jdbcType=INTEGER},#{name,jdbcType=VARCHAR},#{finishtime,jdbcType=VARCHAR},#{partment,jdbcType=VARCHAR},#{badge,jdbcType=INTEGER})
    </insert>

    <insert id="shenBaoKeYanLunWen" parameterType="com.example.graduate_sever.entity.HeBingEntity" useGeneratedKeys="true" keyProperty="id">
        INSERT IGNORE INTO keyanlunwen( `status`, `name`, `finishtime`, `partment`,`shenBao`,`metails`) VALUES (#{status,jdbcType=INTEGER},#{name,jdbcType=VARCHAR},#{finishtime,jdbcType=VARCHAR},#{partment,jdbcType=VARCHAR},#{badge,jdbcType=INTEGER})
    </insert>

    <insert id="insertKeYanLunWenParticipation" parameterType="com.example.graduate_sever.entity.ParticipationEntity" useGeneratedKeys="true">
        INSERT IGNORE INTO participation(`badge`, `ach_id`, `type`) VALUES (#{badge,jdbcType=INTEGER},#{ach_id,jdbcType=INTEGER},#{type,jdbcType=INTEGER})
    </insert>

    <select id="KeYanLunWenMetails" parameterType="java.lang.Integer" resultMap="KeYanLunWenMetails">
        SELECT metails FROM `keyanlunwen` where id=#{id}
    </select>

    <select id="selectKeYanLunWen" parameterType="java.lang.String" resultMap="KeYanLunWen">
        select * from `keyanlunwen` where status=1 and finishtime between #{date1} and #{date2}
    </select>

    <select id="waitingkeyanlunwen" resultMap="KeYanLunWen">
        <bind name="beginIndex" value="(pageIndex-1)*pageSize"></bind>
        select * from `keyanlunwen` where status=0 limit #{beginIndex,jdbcType=INTEGER},#{pageSize,jdbcType=INTEGER}
    </select>

    <update id="passKeYanLunWen" parameterType="java.lang.Integer">
        update `keyanlunwen` set `status`=#{ispass} where id=#{id}
    </update>

    <select id="getKeYanLunWenDisData" resultMap="KeYanLunWenMyShenBaoModel">
        <bind name="beginIndex" value="(pageIndex-1)*pageSize"></bind>
        select c.name,c.id,c.partment,c.finishtime from `keyanlunwen` c,participation p where status=-1 and c.shenbao=p.badge and c.shenbao=#{badge} and p.type=1 limit #{beginIndex,jdbcType=INTEGER},#{pageSize,jdbcType=INTEGER}
    </select>
</mapper>

