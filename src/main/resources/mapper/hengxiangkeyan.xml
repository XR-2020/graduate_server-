<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.graduate_sever.Dao.HengXiangKeYanMapper">
    <!--实体映射-->
    <resultMap id="HengXiangKeYan" type="com.example.graduate_sever.model.HengXiangKeYanXiangMu">
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="status" jdbcType="INTEGER" property="status"/>
        <result column="finishtime" jdbcType="VARCHAR" property="finishtime"/>
        <result column="name" jdbcType="VARCHAR" property="name"/>
        <result column="partment" jdbcType="VARCHAR" property="partment"/>
        <result column="badge" jdbcType="INTEGER" property="badge"/>
        <result column="shenbao" property="shenbao"/>
        <result column="metails" property="metails"/>
    </resultMap>

    <resultMap id="HengXiangKeYanMyShenBaoModel" type="com.example.graduate_sever.common.People">
        <result column="name" property="name"/>
        <result column="id" property="id"/>
        <result column="partment" property="partment"/>
        <result column="finishtime" property="finishtime"/>
    </resultMap>
    <!--参与人映射-->
    <resultMap id="HengXiangKeYan_people" type="com.example.graduate_sever.common.People">
        <result column="badeg" jdbcType="INTEGER" javaType="int" property="badge"/>
        <result column="tea_name" jdbcType="VARCHAR" javaType="string" property="name"/>
    </resultMap>
    <resultMap id="count2" type="java.lang.Long"/>
    <resultMap id="HengXiangKeYanMetails" type="com.example.graduate_sever.common.Metails">
        <result column="metails" property="metails"></result>
    </resultMap>
    <!--查询教研项目所有成果-->
    <!--application.yml 中通过 type-aliases-package 指定了实体类的为了，因此-->
    <select id="getAllHengXiangKeYan" resultMap="HengXiangKeYan,count2">
        <bind name="beginIndex" value="(pageIndex-1)*pageSize"></bind>
        select hengxiangkeyanxiangmu.* from hengxiangkeyanxiangmu where status=1 limit #{beginIndex,jdbcType=INTEGER},#{pageSize,jdbcType=INTEGER};select count(*) count2 from  hengxiangkeyanxiangmu where status=1 ;
    </select>
    <!--删除一个教研项目-->
    <delete id="deleteOneHengXiangKeYan">
        delete * from hengxiangkeyanxiangmu where hengxiangkeyanxiangmu.id=#{id}
    </delete>
    <!--查询指定教研项目成果人员信息-->
    <select id="getHengXiangKeYanDetail" parameterType="java.lang.Integer" resultMap="HengXiangKeYan_people">
        select teacher.tea_name,teacher.badeg from participation,teacher where participation.badge=teacher.badeg and participation.ach_id=#{id} and participation.type=7
    </select>
    <!--教研项目成果全局模糊查询-->
    <select id="getSearchHengXiangKeYan" resultMap="HengXiangKeYan,count2">
        <bind name="beginIndex" value="(pageIndex-1)*pageSize"></bind>
        select c.* from hengxiangkeyanxiangmu c,teacher t,participation p where status=1 and p.badge=t.badeg and p.ach_id=c.id and CONCAT(c.id,t.tea_name,c.partment,c.name,c.finishtime,c.status,p.badge) LIKE CONCAT('%',#{key},'%') limit #{beginIndex,jdbcType=INTEGER},#{pageSize,jdbcType=INTEGER};
        select count(*) count2 from hengxiangkeyanxiangmu c,teacher t,participation p where status=1 and p.badge=t.badeg and p.ach_id=c.id and CONCAT(c.id,t.tea_name,c.partment,c.name,c.finishtime,c.status,p.badge) LIKE CONCAT('%',#{key},'%');
    </select>

    <insert id="insertHengXiangKeYan" parameterType="com.example.graduate_sever.entity.HeBingEntity" useGeneratedKeys="true" keyProperty="id">
        INSERT IGNORE  INTO hengxiangkeyanxiangmu( `status`, `name`, `finishtime`, `partment`,`badge`) VALUES (#{status,jdbcType=INTEGER},#{name,jdbcType=VARCHAR},#{finishtime,jdbcType=VARCHAR},#{partment,jdbcType=VARCHAR},#{badge,jdbcType=INTEGER})
    </insert>

    <insert id="shenBaoHengXiangKeYan" parameterType="com.example.graduate_sever.entity.HeBingEntity" useGeneratedKeys="true" keyProperty="id">
        INSERT IGNORE  INTO hengxiangkeyanxiangmu( `status`, `name`, `finishtime`, `partment`,`shenbao`,`metails`) VALUES (#{status,jdbcType=INTEGER},#{name,jdbcType=VARCHAR},#{finishtime,jdbcType=VARCHAR},#{partment,jdbcType=VARCHAR},#{badge,jdbcType=INTEGER})
    </insert>

    <insert id="insertHengXiangKeYanParticipation" parameterType="com.example.graduate_sever.entity.ParticipationEntity" useGeneratedKeys="true">
        INSERT IGNORE  INTO participation(`badge`, `ach_id`, `type`) VALUES (#{badge,jdbcType=INTEGER},#{ach_id,jdbcType=INTEGER},#{type,jdbcType=INTEGER})
    </insert>
    <select id="HengXiangKeYanMetails" parameterType="java.lang.Integer" resultMap="HengXiangKeYanMetails">
        SELECT metails FROM `hengxiangkeyanxiangmu` where id=#{id}
    </select>
    <select id="selectHengXiangKeYan" parameterType="java.lang.String" resultMap="HengXiangKeYan">
        select * from `hengxiangkeyanxiangmu` where status=1 and finishtime between #{date1} and #{date2}
    </select>

    <select id="waitinghengxiangkeyan" resultMap="HengXiangKeYan">
        <bind name="beginIndex" value="(pageIndex-1)*pageSize"></bind>
        select * from `hengxiangkeyanxiangmu` where status=0 limit #{beginIndex,jdbcType=INTEGER},#{pageSize,jdbcType=INTEGER}
    </select>

    <update id="passHengXiangKeYan" parameterType="java.lang.Integer">
        update `hengxiangkeyanxiangmu` set `status`=#{ispass} where id=#{id}
    </update>

    <select id="getHengXiangKeYanDisData" resultMap="HengXiangKeYanMyShenBaoModel">
        <bind name="beginIndex" value="(pageIndex-1)*pageSize"></bind>
        select c.name,c.id,c.partment,c.finishtime from `hengxiangkeyanxiangmu` c,participation p where status=-1 and c.shenbao=p.badge and c.shenbao=#{badge} and p.type=1 limit #{beginIndex,jdbcType=INTEGER},#{pageSize,jdbcType=INTEGER}
    </select>
</mapper>

